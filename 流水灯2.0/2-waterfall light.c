#include <reg52.h>
#include <intrins.h>
#define uint unsigned int
#define uchar unsigned char

void delay(unsigned int z)
{
		unsigned int x,y;
		for(x = z; x > o; x--)
		for(y = 114; y > 0; y--);
}

void main()
{
					while(1)
					{
						delay(100);
						
					}






}